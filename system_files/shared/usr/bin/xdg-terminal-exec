#!/usr/bin/bash

if command -v /usr/bin/konsole > /dev/null; then
  if [[ -z "$@" ]]; then
    /usr/bin/konsole --new-tab
  else
    /usr/bin/konsole -e "$@"
  fi
else
  /usr/bin/gnome-terminal -- "$@"
fi
