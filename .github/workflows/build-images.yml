name: Build All Images
on:
    workflow_dispatch:

permissions:
    contents: read
    packages: write
    id-token: write

jobs:
    build-image-stable:
        uses: ./.github/workflows/build-image-stable.yml
        secrets: inherit
